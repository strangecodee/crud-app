<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>User Details</title>
    <link href="/output.css" rel="stylesheet" />
  </head>

  <body class="bg-gray-100 min-h-screen py-8">
    <div class="max-w-3xl mx-auto px-4 animate-fade-in-up">
      <a href="/users">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="#2563eb"
          class="size-6"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M10.5 19.5 3 12m0 0 7.5-7.5M3 12h18"
          />
        </svg>
      </a>
      <% if (user) { %>

      <h1
        class="text-center text-3xl font-semibold mb-6 flex items-center justify-center gap-2"
      >
        <!-- Heroicon: Identification -->
        <svg
          xmlns="http://www.w3.org/2000/svg"
          fill="none"
          viewBox="0 0 24 24"
          stroke-width="1.5"
          stroke="#2563eb"
          class="size-8"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M15 9h3.75M15 12h3.75M15 15h3.75M4.5 19.5h15a2.25 2.25 0 0 0 2.25-2.25V6.75A2.25 2.25 0 0 0 19.5 4.5h-15a2.25 2.25 0 0 0-2.25 2.25v10.5A2.25 2.25 0 0 0 4.5 19.5Zm6-10.125a1.875 1.875 0 1 1-3.75 0 1.875 1.875 0 0 1 3.75 0Zm1.294 6.336a6.721 6.721 0 0 1-3.17.789 6.721 6.721 0 0 1-3.168-.789 3.376 3.376 0 0 1 6.338 0Z"
          />
        </svg>

        User #<%= user.id %> Details
      </h1>

      <div class="bg-white shadow-md rounded-lg p-6 mb-6">
        <h4 class="text-xl font-medium mb-4">Basic Information</h4>

        <dl class="divide-y divide-gray-200">
          <div class="py-2 grid grid-cols-3">
            <dt class="font-semibold">ID</dt>
            <dd class="col-span-2">#<%= user.id %></dd>
          </div>

          <div class="py-2 grid grid-cols-3">
            <dt class="font-semibold">Name</dt>
            <dd class="col-span-2"><%= user.name %></dd>
          </div>

          <div class="py-2 grid grid-cols-3">
            <dt class="font-semibold">Email</dt>
            <dd class="col-span-2"><%= user.email %></dd>
          </div>

          <div class="py-2 grid grid-cols-3">
            <dt class="font-semibold">Created At</dt>
            <dd class="col-span-2">
              <%= user.createdAt ? user.createdAt.toLocaleString() : '-' %>
            </dd>
          </div>

          <div class="py-2 grid grid-cols-3">
            <dt class="font-semibold">Last Updated</dt>
            <dd class="col-span-2">
              <%= user.updatedAt ? user.updatedAt.toLocaleString() : '-' %>
            </dd>
          </div>
        </dl>
      </div>

      <% } else { %>
      <h1
        class="text-center text-3xl font-semibold text-red-600 mb-6 flex justify-center items-center gap-2"
      >
        User Not Found
      </h1>

      <div class="text-center">
        <p class="text-gray-700 mb-4">
          The user you are looking for does not exist or may have been removed.
        </p>
      </div>

      <% } %>
    </div>
  </body>
</html>
