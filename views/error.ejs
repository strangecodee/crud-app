<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title || 'Error' %> - User Management</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" />
  <link rel="stylesheet" href="/style.css" />
</head>
<body class="bg-light d-flex align-items-center" style="min-height: 100vh;">
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-7">
        <div class="card shadow-sm p-4">
          <div class="d-flex align-items-center mb-3">
            <i class="bi bi-exclamation-triangle-fill text-danger fs-1 me-3"></i>
            <div>
              <h2 class="mb-1"><%= title || 'Something went wrong' %></h2>
              <p class="text-muted mb-0"><%= message || 'An unexpected error occurred while processing your request.' %></p>
            </div>
          </div>

          <div class="mt-3">
            <a href="/" class="btn btn-primary me-2">
              <i class="bi bi-house-door-fill me-1"></i>
              Back to Dashboard
            </a>
            <a href="/users" class="btn btn-outline-secondary">
              <i class="bi bi-people-fill me-1"></i>
              View Users
            </a>
          </div>

          <% if (process.env && process.env.NODE_ENV !== 'production' && error) { %>
            <hr class="my-4" />
            <p class="text-muted small mb-1">Technical details (development only):</p>
            <pre class="small bg-light border rounded p-2"><%= error.stack || error.message %></pre>
          <% } %>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
